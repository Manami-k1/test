
export type PropsType = {
    children?: React.ReactNode,
    m?: string,
    p?: string,
    bgColor?: string,
    fontSize?: string,
    h?: string,
    bg?: string
}
export type ButtonType = {
    type: string
} & PropsType